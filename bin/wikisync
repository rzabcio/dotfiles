#!/bin/bash

for WIKI_DIR in ~/vimwiki/*; do
	if [ -d "$WIKI_DIR" ] && [ -d "${WIKI_DIR}/.git" ]; then
		git -C "$WIKI_DIR" add .
		git -C "$WIKI_DIR" commit -m \"auto-update\"
		git -C "$WIKI_DIR" pull
		git -C "$WIKI_DIR" push
	fi
done
